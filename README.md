# to run the front:

- cd front-end
- npm i
- npm install framer-motion react-intersection-observer
- npm run dev

---

# to run the back:

- cd back-end
- pip install torch
- uvicorn main:app --reload
